# legacy

`installer.sh` is the original dialog/whiptail installer that shipped on the
first GötünOS ISOs. It is kept here as a reference for the feature set the Qt
installer had to match — it is not maintained and is not used by the ISO
anymore.
